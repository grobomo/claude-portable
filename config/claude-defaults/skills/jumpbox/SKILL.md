# Jumpbox

Manage persistent Windows EC2 test jumpboxes. Config-driven (no hardcoded IDs). Handles RDP credential injection, self-signed cert trust, and auto-dismissal of publisher warnings.

**Triggers:** jumpbox, windows box, rdp, test box, spin up windows, windows jumpbox, remote desktop

## Prerequisites

- AWS CLI configured (`aws configure`)
- Python 3 with `pywinauto` (for RDP auto-connect on Windows)

## First-time setup

If no jumpbox exists yet, run:

```bash
bash ~/.claude/skills/jumpbox/install.sh    # install 'jumpbox' command
jumpbox setup --name jumpbox --timezone "Central Standard Time"
```

This creates: EC2 instance (Win2022, t3.large, 50GB), security group, SSH key, IAM role for SSM, installs Chrome + Git Bash, sets timezone.

## Commands

Run via Bash tool:

| User says | Run |
|-----------|-----|
| "jumpbox" / "rdp" / "connect to windows" | `jumpbox` |
| "stop the jumpbox" | `jumpbox stop` |
| "jumpbox status" | `jumpbox status` |
| "set up a windows jumpbox" | `jumpbox setup` |
| "run X on the jumpbox" | `jumpbox run "powershell command"` |
| "snapshot the jumpbox" | `jumpbox snapshot` |
| "list jumpboxes" | `jumpbox list` |

## How connect works

1. Starts instance if stopped, waits for RDP port
2. Decrypts Windows admin password using the SSH key
3. Stores credentials in Windows Credential Manager via PowerShell (TERMSRV/ prefix)
4. Exports RDP server's self-signed cert via openssl, imports to user trusted root store via certutil
5. Writes .rdp file with `prompt for credentials:i:0`
6. Launches mstsc.exe
7. Background pywinauto script checks "Don't ask me again" + clicks "Connect" on the publisher warning

## Config

Configs stored at `~/.jumpbox/<name>.json` (auto-generated by setup):
```json
{
  "name": "jumpbox",
  "instance_id": "i-xxx",
  "region": "us-east-2",
  "key_path": "~/.ssh/jumpbox-keys/jumpbox.pem",
  "instance_type": "t3.large",
  "timezone": "Central Standard Time"
}
```

No secrets in config -- password is decrypted live from EC2 API using the SSH key.

## What's pre-installed (via setup)

- Windows Server 2022
- Google Chrome (enterprise MSI)
- Git for Windows (includes Git Bash)
- SSM Agent (for remote command execution)
- Timezone set to user's choice (default: CST)

## Important

- Always run commands via Bash tool, not by telling the user
- IP changes on every stop/start -- the script handles this automatically
- Keys are at `~/.ssh/jumpbox-keys/` (never committed, never shared)
- Passwords are never stored on disk -- decrypted live from EC2 metadata
