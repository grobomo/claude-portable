name: list_asrm_vulnerabilities_affected_container_clusters
description: Get container clusters affected by CVEs
method: GET
endpoint: /v3.0/asrm/vulnerabilities/{id}/affectedContainerClusters
template: standard_list
tags:
- Threat and Exposure Management
params:
  required:
  - name: id
    type: string
    description: The CVE ID
  optional:
  - name: top
    type: integer
    description: 'The number of records displayed on a page


      Default: `100`

      '
template_config:
  pagination:
    param: top
    type: int
    max: 200
