name: list_container_security_rulesets
description: Get all rulesets
method: GET
endpoint: /v3.0/containerSecurity/rulesets
template: single_get
tags:
- Rulesets
params:
  required: []
  optional:
  - name: null
    type: string
    description: ''
  - name: TMV1-Filter
    type: string
    description: "The filter for retrieving a subset of the rulesets list. Include\
      \ this parameter in every request that generates paginated output\n\nSupported\
      \ fields: \n* `name` - The name of the ruleset\n* `type` - The type of the ruleset.\
      \ Supported values:  `userManaged`, `clusterManaged`\n\nSupported operators\n\
      * `eq` - Operator \"equal to\"\n* `contains` - Operator that allows you to search\
      \ for a specified string in a field\n* `or` - Operator \"or\"\n* `not` - Operator\
      \ \"not\"\n"
  - name: orderBy
    type: string
    description: 'The fields by which the results are sorted. You can indicate multiple
      fields separated by commas (`,`).


      To return records in descending order, add the keyword `desc` after the parameter
      name.


      Available fields:

      * `createdDateTime`

      * `updatedDateTime`

      '
template_config: {}
