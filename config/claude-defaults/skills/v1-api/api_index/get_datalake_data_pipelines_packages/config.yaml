name: get_datalake_data_pipelines_packages
description: 'Get package

  '
method: GET
endpoint: /v3.0/datalake/dataPipelines/{id}/packages/{packageId}
template: single_get
tags:
- Datalake Pipeline
params:
  required:
  - name: id
    type: string
    description: 'The ID of a data pipeline.

      '
  - name: packageId
    type: string
    description: 'The ID of a package stored in a data pipeline.

      '
  optional: []
template_config: {}
