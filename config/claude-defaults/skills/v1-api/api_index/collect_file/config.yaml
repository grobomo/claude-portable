name: collect_file
endpoint: /v3.0/response/endpoints/collectFile
method: POST
template: response_action
description: Collect file from endpoint
params:
  required:
  - name: endpoint_guid
    type: str
    description: Endpoint agent GUID
  - name: file_path
    type: str
    description: Full file path
  optional:
  - name: description
    type: str
    description: Reason
    default: Collected via MCP
template_config:
  pagination: null
  date_params: null
  filter_style: null
  body_builder: file_collect
categories:
- response_actions
- incident_response
