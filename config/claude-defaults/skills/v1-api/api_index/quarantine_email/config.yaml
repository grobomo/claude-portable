name: quarantine_email
endpoint: /v3.0/response/emails/quarantine
method: POST
template: response_action
description: Quarantine email message
params:
  required:
  - name: message_id
    type: str
    description: Email message ID
  - name: mailbox
    type: str
    description: Mailbox address
  optional:
  - name: description
    type: str
    description: Reason
    default: Quarantined via MCP
template_config:
  pagination: null
  date_params: null
  filter_style: null
  body_builder: email_action
categories:
- response_actions
- incident_response
