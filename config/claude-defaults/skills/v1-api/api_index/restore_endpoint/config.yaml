name: restore_endpoint
endpoint: /v3.0/response/endpoints/restore
method: POST
template: response_action
description: Restore isolated endpoint
params:
  required:
  - name: endpoint_guid
    type: str
    description: Endpoint agent GUID
  optional:
  - name: description
    type: str
    description: Reason
    default: Restored via MCP
template_config:
  pagination: null
  date_params: null
  filter_style: null
  body_builder: endpoint_action
categories:
- response_actions
- incident_response
