# Worker PR Dashboard

## Goal
Show a list of workers and what PR they are actively working on in the fleet dashboard.

## Success Criteria
1. Dashboard shows each worker with their current active PR (number, title, branch)
2. Workers without an active PR show "idle" or "no PR"
3. Data updates on the 15-second auto-refresh cycle
4. Existing tests continue to pass
