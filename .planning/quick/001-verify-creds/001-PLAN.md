# Verify Credentials Work

## Goal
Verify that the dispatched worker can create branches, push, and open PRs using the configured GitHub credentials.

## Success Criteria
- [x] Create a branch from main
- [x] Commit a change
- [x] Push to origin
- [x] Create a PR via `gh pr create`
